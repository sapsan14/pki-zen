% PKI-ZEN XeLaTeX template.
%
% Fonts are chosen for CI reliability, not typographic elegance:
% - Noto Serif has Latin, Cyrillic, and all Estonian diacritics (Š Ž Õ Ä Ö Ü).
% - Noto Sans for headings.
% - DejaVu Sans Mono is always available via fonts-dejavu on Ubuntu.
%
% If you prefer EB Garamond, install it and swap \setmainfont below.

\documentclass[12pt,oneside]{book}

\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{$babel-lang$}
\setotherlanguages{russian,estonian,english}

\setmainfont{Noto Serif}[Ligatures={TeX,Common}]
\setsansfont{Noto Sans}
\setmonofont{DejaVu Sans Mono}[Scale=0.88]

\usepackage{geometry}
\geometry{paperwidth=152mm, paperheight=229mm, margin=18mm}

\usepackage{microtype}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=black, urlcolor=black, citecolor=black}

\usepackage{amsmath}
\usepackage{enumitem}
\setlist{nosep}

% Allow long URLs and command lines to break inside code blocks.
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  breaklines,breakanywhere,commandchars=\\\{\},fontsize=\small
}

% We build PDFs with --no-highlight (see publish/build.sh), so Pandoc
% does not emit Shaded / Tok macros. Code blocks render in plain
% monospace via the Highlighting Verbatim env above.

% Pandoc emits a handful of command-name "providers" the default
% template normally defines; our custom template must supply them
% too, otherwise LaTeX dies on undefined control sequences.
% Using \providecommand so that any future Pandoc version that
% starts defining them itself wins cleanly.
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\passthrough}[1]{#1}
\providecommand{\pandocbounded}[1]{#1}

% Quiet, book-like chapter titles.
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\bfseries\filcenter}
  {\fontsize{14pt}{16pt}\selectfont\textsc{\chaptertitlename\ \thechapter}}
  {12pt}
  {\fontsize{22pt}{26pt}\selectfont}
\titlespacing*{\chapter}{0pt}{20pt}{32pt}

% Some metadata placeholders Pandoc may emit.
$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(date)$
\date{$date$}
$endif$

\begin{document}
$if(title)$
\maketitle
$endif$

$if(toc)$
\tableofcontents
\clearpage
$endif$

$body$

\end{document}
